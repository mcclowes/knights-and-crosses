# Server Configuration
PORT=3000
NODE_ENV=development
LOG_LEVEL=info

# Game Configuration
MAX_PLAYERS_PER_GAME=2
GAME_TIMEOUT=300000  # 5 minutes in milliseconds

# Security
CORS_ORIGIN=http://localhost:3000

# WebSocket Server URL (for hybrid deployment with Vercel + Railway)
# For local development: http://localhost:3000
# For Railway production: https://your-app-name.railway.app
# Leave blank for same-origin (if deploying as single server)
NEXT_PUBLIC_WS_URL=http://localhost:3000

# Redis Configuration (Vercel Marketplace Integration)
# Required for production persistence
# Get this from: https://vercel.com/dashboard (Integrations → Redis)
# See: https://vercel.com/docs/storage
# Format: redis://default:password@hostname:port
KV_URL=redis://default:your_password@your-redis-host.com:6379 
