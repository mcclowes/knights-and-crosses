# Server Configuration
PORT=3000
NODE_ENV=development
LOG_LEVEL=info

# Game Configuration
MAX_PLAYERS_PER_GAME=2
GAME_TIMEOUT=300000  # 5 minutes in milliseconds

# Security
CORS_ORIGIN=http://localhost:3000

# WebSocket Server URL (for hybrid deployment with Vercel + Railway)
# For local development: http://localhost:3000
# For Railway production: https://your-app-name.railway.app
# Leave blank for same-origin (if deploying as single server)
NEXT_PUBLIC_WS_URL=http://localhost:3000

# Vercel KV Configuration (Redis)
# Required for production persistence
# Get these from: https://vercel.com/dashboard/stores
# See: https://vercel.com/docs/storage/vercel-kv/quickstart
KV_REST_API_URL=https://your-kv-instance.vercel-storage.com
KV_REST_API_TOKEN=your_kv_token_here
KV_REST_API_READ_ONLY_TOKEN=your_kv_read_only_token
KV_URL=redis://default:token@endpoint:port 
